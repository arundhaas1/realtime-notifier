server:
  port: 8080

spring:
  application:
    name: notifier
  threads:
    virtual:
      enabled: true        # Java 21 virtual threads — load-bearing for the design

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics, prometheus
  endpoint:
    health:
      show-details: always
